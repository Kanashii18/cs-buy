"use client";

import Container from "../../components/options/container";
import UseMain from "../../components/main_options";

export default function Profile_content(){

     // ===============|| send the component of the rute to the main content ||================ //
     
     return <UseMain Main_content={Container}/>;
}