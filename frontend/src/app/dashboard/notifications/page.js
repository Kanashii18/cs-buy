"use client";

import Notifications from "../../components/options/notifications/container";
import UseMain from "../../components/main_options";

export default function Notifications_content(){

     // ===============|| send the component of the rute to the main content ||================ //
     
     return <UseMain Main_content={Notifications}/>;
}