"use client";

import Settings from "../../components/options/settings/content";
import UseMain from "../../components/main_options";

export default function Settings_content(){

     // ===============|| send the component of the rute to the main content ||================ //
     
     return <UseMain Main_content={Settings}/>;
}